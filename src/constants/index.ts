import * as JsonAppMessages from './app-messages.json';
import * as JsonSwaggerMessages from './swagger-messages.json';

export const AppMessages = JsonAppMessages;
export const SwaggerMessages = JsonSwaggerMessages;
